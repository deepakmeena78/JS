<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="../javascript/main.js"></script>
    <title>Document</title>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <a style="color: rgb(28, 186, 214); font-size: 30px; border: 2px solid rgb(49, 224, 255); border-radius: 8px; padding: 10px;"
            href="/create"> Add User</a>
        <h1 style="margin: 10px;" class="text-3xl tracking-tighter mb-5">All Users</h1>
        <div class="Users flex gap-3 flex-grap">
            <% if(yo.length> 0) { %>
                <% yo.forEach((yo, index)=> { %>
                    <div class="user max-w-xs p-4 bg-zinc-800 rounded-lg">
                        <img src="<%= yo.image %>" alt="User's image">
                        <h3 class="text-xl mt-3 tracking-tighter">
                            <%= yo.name %>
                        </h3>
                        <h3 class="text-zinc-500">
                            <%= yo.email %>
                        </h3>
                        <div class="flex gap-3 mt-5">
                            <!-- <a id="adduser" href="/create" style="color: rgb(0, 240, 44);"
                                class="hover:text-green-400">Add
                                User</a> -->
                            <a href="/edit/<%= yo._id %>" style="color: rgb(28, 186, 214);"
                                class="hover:text-red-700">Update User</a>
                            <a href="/delete/<%= yo._id %>" class="text-red-500 hover:text-red-700">Delete User</a>
                        </div>
                    </div>
                    <% }); %>
                        <% } else { %>
                            <p>No users found
                                <br>
                                <br>
                            </p>
                            <% } %>
        </div>
    </div>
</body>

</html>